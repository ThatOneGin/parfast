Reserved = {
  PUSH_INT  = enum(true),
  PUSH_STR  = enum(),
  PUTS      = enum(),
  ADD       = enum(),
  SUB       = enum(),
  IF        = enum(),
  END       = enum(),
  EQU       = enum(),
  NEQ       = enum(),
  LT        = enum(),
  GT        = enum(),
  DUP       = enum(),
  SWAP      = enum(),
  WHILE     = enum(),
  DO        = enum(),
  ELSE      = enum(),
  MBUF      = enum(),
  LOAD      = enum(),
  STORE     = enum(),
  DROP      = enum(),
  MACRO     = enum(),
  INCLUDE   = enum(),
  MUL       = enum(),
  DIV       = enum(),
  ENDM      = enum(),
  ROT       = enum(),
  RST       = enum(),
  RLD       = enum(),
  EXTERN    = enum(),
  CALL      = enum(),
  ELSEIF    = enum(),
  THEN      = enum(),
  SYSCALL0  = enum(),
  SYSCALL1  = enum(),
  SYSCALL4  = enum(),
  SYSCALL2  = enum(),
  SYSCALL6  = enum(),
  SYSCALL3  = enum(),
  SYSCALL5  = enum(),
  MEM       = enum(),
  MOD       = enum(),
  ARGC      = enum(),
  ARGV      = enum(),
  FN        = enum(),
  FN_BODY   = enum(),
  RET       = enum(),
  FN_CALL   = enum(),
  LOCAL_MEM = enum(),
  ASM       = enum(),
  ENDBIND   = enum(),
  IN        = enum(),
  PUSHBIND  = enum(),
  WITH      = enum()
}

return Reserved